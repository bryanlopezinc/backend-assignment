# Vessels Tracks API

## System Requirements

* php ^8.1
* mbstring extension

## Installation

* clone repository
* run `composer install`
* run `php artisan migrate`
* run `php artisan db:seed`
* run `php artisan test`